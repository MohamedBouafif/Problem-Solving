#include <bits/stdc++.h>
using namespace std;
using ll = long long;

void solve()
{
    int w, h, b, a;
    cin >> w >> h >> a >> b;
    int x1, y1, x2, y2;
    cin >> x1 >> y1 >> x2 >> y2;
    if (x1 > x2)
    {
        swap(x1, x2);
        swap(y1, y2);
    }
   
    
}
int main()
{
    int t = 1;
    cin >> t;
    while (t--)
    {
        solve();
    }
}